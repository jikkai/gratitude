import * as React from 'react'
import { Link } from 'react-router-dom'
import './style'

class Profile extends React.Component<undefined, any> {
  constructor (props: any) {
    super(props)
  }

  render () {
    return (
      <section className="period-profile">
        Profile
      </section>
    )
  }
}

export default Profile
